server:
  port: 8082

spring:
  application:
    name: profile-service

  data:
    mongodb:
      uri: mongodb://admin:password@localhost:27017/profile_db?authSource=admin

  threads:
    virtual:
      enabled: true

jwt:
  secret: ${JWT_SECRET}